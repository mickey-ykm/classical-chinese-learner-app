// Stub tailwind config to prevent NativeWind from throwing when scanning admin/ directory.
// This folder is a Node.js Express server, not a React Native project.
module.exports = {
  content: [],
}
