# Marked as completed on 13 June 2026


1. For these 3 question types, the answer options should shuffle everytime when the user start taking the test:
| `mc-single` | 選擇題（單選）| `mc` | 1 of 4 | Correct = full points |
| `mc-multi` | 選擇題（多選）| `mc` | 2–5 of 4–8 options | All-or-nothing |
| `sentence-order` | 重組句子/語序 | `sentence-order` | drag-to-arrange | Exact sequence only |
2. I would like to re-originse 4 main sections in the app in a bottom navigation bar:
2a. **Home**: Greetings, Preview of exercise history, Account login.
2b. **DSE Learner**: The single article exercise which the `article type` is either `dse-exam` / `dse-non-exam`.
2c. **Extra Article exercises**: The single article exercise which the `article type` is `other`.
2d. **DSE Training**: the existing one.
2e. **Weight Training**: the existing coming soon.